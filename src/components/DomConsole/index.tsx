import React, { useState } from "react";

const DomConsole = () => {
  const [isOpen, setOpen] = useState(true);
  return <div className={"fixed left-0 top-0 "}></div>;
};

export default DomConsole;
